{
  "name": "api",
  "version": "0.0.1",
  "description": "Personal schedule api",
  "main": "dist/sever.js",
  "scripts": {
    "dev": "ts-node-dev --respawn src/server.ts",
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "4",
    "mongoose": "^6.2.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.23",
    "dotenv": "^16.0.0",
    "ts-node": "^10.7.0",
    "ts-node-dev": "^1.1.8",
    "tslint": "^6.1.3",
    "typescript": "^4.6.3"
  }
}
